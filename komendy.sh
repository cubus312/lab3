https://github.com/cubus312/lab3

git commit, git commit--ammend
git branch
git checkout, git checkout -b
git merge
git rebase, git rebase -i
git branch -f
git reset, git reset --hard
git revert
git cherry-pick
git tag
git push
git pull